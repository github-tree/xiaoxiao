<template><div><p>因为空结构体不占据内存空间，因此被广泛作为各种场景下的占位符使用。</p>
<blockquote>
<ol>
<li>将 map 作为集合(Set)使用时，可以将值类型定义为空结构体，仅作为占位符使用即可。</li>
<li>不发送数据的channel</li>
<li>结构体只包含方法，不包含任何的字段</li>
</ol>
</blockquote>
</div></template>


