<template><div><p>多个key失效，造成大量请求到db层，导致db层负担过重甚至宕机。</p>
<ul>
<li>
<p>方案一：使用互斥锁的方式，保证只有单个线程进行请求能够达到db；</p>
</li>
<li>
<p>方案二：多每个key的失效时间在基础时间上再加上一个1~5分钟的随机值，这样就能保证大规模key集体失效的概率，并且需要尽量让多个key的失效时间能够均匀分布；</p>
</li>
</ul>
</div></template>


