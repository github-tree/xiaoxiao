<template><div><ul>
<li>
<p>CHAR(N) 用来保存固定长度的字符，N 的范围是 0 ~ 255，注意，N 表示的是字符，不是字节。</p>
</li>
<li>
<p>VARCHAR(N) 用来保存变长字符，N 的范围为 0 ~ 65536， N 表示字符。</p>
</li>
</ul>
<p>超出 65536 个字符时，可以使用更大的字符类型 TEXT 或 BLOB，两者最大存储长度为 4G，其区别是 BLOB 没有字符集属性，纯属二进制存储。</p>
</div></template>


